<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:extractNativeLibs="true"
        android:theme="@style/Theme.ARApplication">

        <meta-data 
            android:name="com.google.ar.core" 
            android:value="optional"
            tools:replace="android:value" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data android:name="io.sentry.dsn" android:value="https://a7c05d53715cf162cdd1166c1ef9c5dd@o4510872677908480.ingest.us.sentry.io/4511543560962048" />
        <meta-data android:name="io.sentry.send-default-pii" android:value="true" />

        <!-- Splash Screen (LAUNCHER) -->
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (redirect) -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- AR Activity -->
        <activity
            android:name=".ARActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Pseudo AR Activity (Fallback for non-ARCore devices) -->
        <activity
            android:name=".PseudoARActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- PDF Viewer Activity -->
        <activity
            android:name=".presentation.pdf.PdfViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.ARApplication"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Chatbot Activity -->
        <activity
            android:name=".presentation.chatbot.ChatbotActivity"
            android:exported="false"
            android:theme="@style/Theme.ARApplication"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>

</manifest>
